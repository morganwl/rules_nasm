load("@bazel_skylib//rules:build_test.bzl", "build_test")

build_test(
    name = "smoke_test",
    targets = [
        "@rules_nasm_smoke//:smoke_test",
    ],
    visibility = ["//visibility:public"],
)
